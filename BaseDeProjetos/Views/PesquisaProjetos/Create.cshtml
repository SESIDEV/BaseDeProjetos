@model BaseDeProjetos.Models.PesquisaProjeto

@{
    ViewData["Title"] = "Create";
    SelectList ListProjetos = ViewData["Projetos"] as SelectList;
}

<h1>Create</h1>

<h4>PesquisaProjeto</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="ProjetoId" class="control-label"></label>
                <select asp-for="ProjetoId" class="form-control" asp-items='@ListProjetos'>
                    <option value="-1">Selecione o projeto</option>
                </select>
            </div>
             <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
    }
